name: release
on:
  pull_request:
    types: [labeled]
    branches:
      - 'master'
permissions:
  id-token: write
  contents: read
jobs:
  release:
    uses: DispatchMe/github-actions/.github/workflows/release-v2.yml@master
    secrets: inherit
